// Mapping of example names to their display names and log file paths
export const exampleLogs: Record<string, { name: string; path: string }> = {
  ruby_build: { name: 'Ruby build', path: './ruby_build.log' },
  uv_build: { name: 'uv build', path: './uv_build.log' },
};
